#interface is a class that includes only abstract methods

#abstract class can have both abstract and implemented methods while interface contains only abstract

#for an interface, subclasses must implement all methods themselves, while abstract class subclasses can override or extend base behavior

#__len__ is len() for a class
#__contains__ sees if something is in